kind: Deployment
apiVersion: apps/v1
metadata:
  name: sonarqube
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sonarqube
  template:
    metadata:
      labels:
        app: sonarqube
    spec:
      containers:
      - name: sonarqube
        image: sonarqube:6.7.3-alpine
        ports:
        - containerPort: 9000
        - containerPort: 9092
        volumeMounts:
        - name: sonarqube-data
          mountPath: /opt/sonarqube/data
          subPath: sonarqube
      volumes:
      - name: sonarqube-data
        persistentVolumeClaim:
          claimName: tool-persistent-volume-claim
